# Course Schedule

There are `numCourses` courses labeled from 0 through `numCourses - 1`.
Each pair `[a, b]` in `prerequisites` means that course `b` must be taken before
course `a`. Return whether it is possible to finish every course.

There are between 1 and 2000 courses and at most 5000 unique prerequisite
pairs.
