# 3Sum

Given an integer array, return every distinct value triplet whose members come
from three distinct indices and sum to zero. The result must not contain
duplicate triplets; the order of triplets and the order within each triplet do
not matter.

The array length is between 3 and 3000, and every value is between `-10^5` and
`10^5`.
