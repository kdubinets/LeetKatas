# Permutations II

Given an integer array that may contain repeated values, return every distinct
permutation of its values in any order. The result must not repeat the same
value sequence.

The array has between 1 and 8 elements, and each value is between `-10` and
`10`.
