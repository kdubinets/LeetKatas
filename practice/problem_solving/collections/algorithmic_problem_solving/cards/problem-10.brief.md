# Regular Expression Matching

Determine whether an entire lowercase input string matches a pattern containing
lowercase letters plus two operators: `.` matches any single character, and `*`
matches zero or more occurrences of the valid pattern element immediately
before it. Matching must cover the whole input rather than a substring.

Both strings have lengths from 1 to 20. Every `*` has a valid preceding
element.
