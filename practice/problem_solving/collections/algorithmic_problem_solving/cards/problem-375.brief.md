# Guess Number Higher or Lower II

A hidden number is chosen from 1 through `n`. You repeatedly guess a number;
a correct guess costs nothing, while an incorrect guess `x` costs `x` dollars
and tells you whether the hidden number is higher or lower. Return the minimum
amount of money needed to guarantee a win regardless of the hidden number.

`n` is between 1 and 200.
