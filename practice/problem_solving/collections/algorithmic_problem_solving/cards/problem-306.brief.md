# Additive Number

Given a string of digits, return whether it can be partitioned into an additive
sequence of at least three numbers: after the first two, each number must equal
the sum of the preceding two. No number may have a leading zero unless it is
zero itself.

The string has between 1 and 35 digits.
