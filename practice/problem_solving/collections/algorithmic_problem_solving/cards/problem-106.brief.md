# Construct Binary Tree from Inorder and Postorder Traversal

You are given the inorder and postorder traversals of the same binary tree.
Construct and return that tree. Both arrays have the same length, contain
unique values between `-3000` and `3000`, and are guaranteed to describe a
valid tree.

Their length is between 1 and 3000.
