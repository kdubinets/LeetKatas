# Name

Sum an Argument Pack

# Description

Return the sum of all by-value function arguments with an `int` zero as the initial value. The same definition must accept an empty pack and return zero.

# Solution

```cpp
return (0 + ... + values);
```
