#include <forward_list>

void solve(
    std::forward_list<int>& destination,
    std::forward_list<int>::const_iterator destination_predecessor,
    std::forward_list<int>& source,
    std::forward_list<int>::const_iterator source_predecessor) {
    // Finish: transfer the node after the source predecessor to the requested destination position
}
