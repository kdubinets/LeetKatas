# Name

Merge Sorted Lists

# Description

Merge the distinct ascending `std::list<int>` objects into one ascending sequence in `destination`. Transfer nodes without copying or moving their values, preserve the order of equivalent elements from each list, and leave `source` empty. This exercise covers the list-owned linear merge operation for already sorted node sequences.

# Solution

```cpp
destination.merge(source);
```
