#include <algorithm>
#include <forward_list>
#include <iterator>
#include <vector>

void solve(std::forward_list<int>& destination,
           const std::vector<int>& source) {
    // Finish: prepend the source so its last value becomes the new first value
}
