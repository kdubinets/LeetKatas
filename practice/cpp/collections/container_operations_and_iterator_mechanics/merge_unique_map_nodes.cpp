#include <map>
#include <string>

void solve(std::map<int, std::string>& destination,
           std::map<int, std::string>& source) {
    // Finish: transfer source entries whose keys are absent from the destination
}
