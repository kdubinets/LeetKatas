#include <list>

void solve(std::list<int>& destination,
           std::list<int>::const_iterator position,
           std::list<int>& source) {
    // Finish: transfer every source element before the supplied destination position
}
