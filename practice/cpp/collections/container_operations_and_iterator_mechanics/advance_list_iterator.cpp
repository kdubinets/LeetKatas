#include <cstddef>
#include <iterator>
#include <list>

using Iterator = std::list<int>::iterator;

Iterator solve(Iterator position, std::size_t steps) {
    // Finish: move the position forward by the validated number of elements
}
