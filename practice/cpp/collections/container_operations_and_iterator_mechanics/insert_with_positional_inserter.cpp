#include <algorithm>
#include <cstddef>
#include <iterator>
#include <list>
#include <string>
#include <vector>

void solve(std::list<std::size_t>& destination,
           std::list<std::size_t>::iterator position,
           const std::vector<std::string>& source) {
    // Finish: insert each source string's length in order before the stable destination position
}
