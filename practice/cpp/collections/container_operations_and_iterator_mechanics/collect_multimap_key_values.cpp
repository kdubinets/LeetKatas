#include <map>
#include <string>
#include <vector>

std::vector<std::string> solve(
    const std::multimap<int, std::string>& values,
    int key) {
    // Finish: return every mapped value for the key in container order
}
