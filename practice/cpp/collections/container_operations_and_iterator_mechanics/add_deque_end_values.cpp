#include <deque>

void solve(std::deque<int>& values, int front_value, int back_value) {
    // Finish: place the specified values at the corresponding ends while preserving all existing values in order
}
