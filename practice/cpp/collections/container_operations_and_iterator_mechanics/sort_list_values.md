# Name

Sort List Values

# Description

Arrange a mutable `std::list<int>` in ascending order. The list's bidirectional iterators do not satisfy the random-access requirement of the general sorting algorithm, so this exercise covers the container-owned stable sorting operation that relinks list nodes.

# Solution

```cpp
values.sort();
```
