#include <iterator>
#include <vector>

std::vector<int>::iterator solve(
    std::vector<int>& values,
    std::vector<int>::const_reverse_iterator position) {
    // Finish: erase the element selected by the reverse position and return its forward successor
}
