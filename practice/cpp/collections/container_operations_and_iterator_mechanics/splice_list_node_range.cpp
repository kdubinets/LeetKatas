#include <list>

void solve(std::list<int>& destination,
           std::list<int>::const_iterator position,
           std::list<int>& source,
           std::list<int>::const_iterator first,
           std::list<int>::const_iterator last) {
    // Finish: transfer the supplied half-open source range before the destination position
}
