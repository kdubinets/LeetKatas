#include <cstddef>
#include <utility>
#include <vector>

void move_zeroes_to_end(std::vector<int>& values) {
    // Pattern: read/write pointers. The prefix before write contains the nonzero values in their original order.
    // Finish: move every zero to the end of values while preserving the relative order of nonzero values
}
