#include <cstddef>

consteval int allocated_sum() {
    // Finish: allocate the values 2, 3, and 5 temporarily, compute their total, release the storage, and return the total
}

static_assert(allocated_sum() == 10);
