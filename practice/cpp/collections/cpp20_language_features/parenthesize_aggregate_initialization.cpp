struct Point {
    int x;
    int y;
};

Point solve(int x, int y) {
    // Finish: directly initialize the aggregate from the two values using parentheses
}

int main() {
    const auto point = solve(3, 8);
    return point.x == 3 && point.y == 8 ? 0 : 1;
}
