# Name

Parenthesize Aggregate Initialization

# Description

Return the aggregate `Point` direct-initialized from `x` and `y` with C++20 parenthesized aggregate initialization rather than braces.

# Solution

```cpp
return Point(x, y);
```
