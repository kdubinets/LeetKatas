#include <filesystem>
#include <system_error>

void solve(
    const std::filesystem::path& source,
    const std::filesystem::path& destination,
    std::error_code& error) {
    // Finish: recursively copy the directory tree and replace existing regular destination files without throwing
}
