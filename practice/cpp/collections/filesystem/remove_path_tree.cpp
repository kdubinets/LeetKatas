#include <cstdint>
#include <filesystem>
#include <system_error>

std::uintmax_t solve(
    const std::filesystem::path& value,
    std::error_code& error) {
    // Finish: recursively remove value and return the number of entries removed without throwing
}
