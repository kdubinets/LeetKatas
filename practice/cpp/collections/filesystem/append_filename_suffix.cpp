#include <filesystem>

std::filesystem::path solve(
    std::filesystem::path value,
    const std::filesystem::path& suffix) {
    // Finish: append suffix directly to value without adding a path separator
}
