#include <filesystem>

std::filesystem::path solve(
    const std::filesystem::path& value,
    const std::filesystem::path& base) {
    // Finish: express value relative to base using only their lexical components
}
