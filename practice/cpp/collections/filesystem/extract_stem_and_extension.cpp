#include <filesystem>
#include <utility>

std::pair<std::filesystem::path, std::filesystem::path> solve(
    const std::filesystem::path& value) {
    // Finish: return the final filename's stem and extension separately
}
