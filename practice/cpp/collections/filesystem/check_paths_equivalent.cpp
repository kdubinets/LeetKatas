#include <filesystem>
#include <system_error>

bool solve(
    const std::filesystem::path& left,
    const std::filesystem::path& right,
    std::error_code& error) {
    // Finish: report whether both paths identify the same object, placing failure in error
}
