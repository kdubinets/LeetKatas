#include <filesystem>
#include <system_error>

bool solve(
    const std::filesystem::path& value,
    std::error_code& error) {
    // Finish: report whether value exists while placing any query failure in error
}
