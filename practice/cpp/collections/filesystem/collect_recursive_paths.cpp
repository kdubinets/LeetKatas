#include <filesystem>
#include <vector>

std::vector<std::filesystem::path> solve(
    const std::filesystem::path& directory) {
    // Finish: return the path of every descendant visited recursively
}
