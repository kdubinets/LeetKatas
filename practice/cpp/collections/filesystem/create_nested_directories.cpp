#include <filesystem>
#include <system_error>

bool solve(
    const std::filesystem::path& value,
    std::error_code& error) {
    // Finish: create every missing directory component and report whether anything was created
}
