#include <filesystem>

std::filesystem::path solve(
    std::filesystem::path value,
    const std::filesystem::path& filename) {
    // Finish: return value with its final filename replaced by filename
}
