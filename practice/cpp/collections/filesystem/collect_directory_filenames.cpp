#include <filesystem>
#include <vector>

std::vector<std::filesystem::path> solve(
    const std::filesystem::path& directory) {
    // Finish: return the filename of every direct child in iterator order
}
