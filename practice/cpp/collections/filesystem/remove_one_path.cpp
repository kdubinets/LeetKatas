#include <filesystem>
#include <system_error>

bool solve(
    const std::filesystem::path& value,
    std::error_code& error) {
    // Finish: remove the single named entry without throwing and report whether it existed
}
