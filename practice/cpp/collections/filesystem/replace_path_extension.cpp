#include <filesystem>

std::filesystem::path solve(
    std::filesystem::path value,
    const std::filesystem::path& extension) {
    // Finish: return value with its final extension replaced by extension
}
