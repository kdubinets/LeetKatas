#include <filesystem>
#include <system_error>

bool solve(
    const std::filesystem::path& source,
    const std::filesystem::path& destination,
    std::error_code& error) {
    // Finish: copy source to destination, replacing an existing destination, without throwing
}
