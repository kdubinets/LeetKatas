#include <cstdint>
#include <filesystem>
#include <system_error>

std::uintmax_t solve(
    const std::filesystem::path& value,
    std::error_code& error) {
    // Finish: return the bytes available to an unprivileged process on value's filesystem
}
