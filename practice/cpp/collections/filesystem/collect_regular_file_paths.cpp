#include <filesystem>
#include <vector>

std::vector<std::filesystem::path> solve(
    const std::filesystem::path& directory) {
    // Finish: return the full paths of direct children that resolve to regular files
}
