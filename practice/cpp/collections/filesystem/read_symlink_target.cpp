#include <filesystem>
#include <system_error>

std::filesystem::path solve(
    const std::filesystem::path& value,
    std::error_code& error) {
    // Finish: return the target stored in the symbolic link while placing failure in error
}
