#include <filesystem>

std::filesystem::path solve(
    const std::filesystem::path& base,
    const std::filesystem::path& child) {
    // Finish: return child joined beneath base using path component rules
}
