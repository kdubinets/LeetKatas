#include <filesystem>
#include <vector>

std::vector<std::filesystem::path> solve(
    const std::filesystem::path& directory,
    const std::filesystem::path& skipped_name) {
    // Finish: collect descendants but do not enter directories with the skipped filename
}
