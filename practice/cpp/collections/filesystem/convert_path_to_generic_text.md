# Name

Convert a Path to Generic Text

# Description

Convert a path to a narrow `std::string` in generic path format, which uses forward slashes as separators. This exercise distinguishes portable serialized path text from the platform's native format.

# Solution

```cpp
return value.generic_string();
```
