#include <filesystem>
#include <fstream>
#include <ios>
#include <optional>
#include <utility>

std::optional<std::ofstream> solve(const std::filesystem::path& path) {
    // Finish: return an output stream that replaces any existing text
}
