#include <algorithm>
#include <istream>
#include <iterator>
#include <ostream>

bool solve(std::istream& source, std::ostream& destination) {
    // Finish: transfer all remaining characters and report whether the destination accepted them
}
