#include <cstddef>
#include <ostream>
#include <span>

bool solve(std::ostream& output, std::span<const std::byte> source) {
    // Finish: write every source byte and report whether the transfer succeeded
}
