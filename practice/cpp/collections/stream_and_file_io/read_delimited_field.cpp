#include <istream>
#include <optional>
#include <string>

std::optional<std::string> solve(std::istream& input, char delimiter) {
    // Finish: return the next field ending at the requested separator
}
