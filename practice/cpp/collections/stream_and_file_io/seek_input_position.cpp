#include <ios>
#include <istream>
#include <optional>

std::optional<std::streampos> solve(std::istream& input,
                                    std::streampos position) {
    // Finish: move to the requested absolute stream position and report the result
}
