#include <ios>
#include <istream>
#include <optional>

std::optional<std::streampos> solve(std::istream& input) {
    // Finish: return the position immediately after the contents of this seekable input
}
