#include <istream>
#include <limits>
#include <optional>
#include <string>
#include <utility>

std::optional<std::pair<int, std::string>> solve(std::istream& input) {
    // Finish: return the next integer and the complete line from the following record
}
