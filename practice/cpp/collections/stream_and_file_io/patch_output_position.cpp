#include <ios>
#include <ostream>

bool solve(std::ostream& output, std::streampos position, char value) {
    // Finish: replace the character at the supplied position and report whether it succeeded
}
