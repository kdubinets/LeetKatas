#include <cstddef>
#include <istream>
#include <span>

bool solve(std::istream& input, std::span<std::byte> destination) {
    // Finish: fill the destination and report whether every requested byte was obtained
}
