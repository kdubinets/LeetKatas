#include <ostream>
#include <string_view>
#include <syncstream>

void write_record(std::ostream& output,
                  std::string_view label,
                  int value) {
    // Finish: write the label, an equals sign, the value, and a newline as one non-interleaved emission
}
