#include <functional>

int convert(int value) {
    return value;
}

double convert(double value) {
    return value;
}

std::function<int(int)> solve() {
    // Finish: return the overload that accepts and returns an integer
}
