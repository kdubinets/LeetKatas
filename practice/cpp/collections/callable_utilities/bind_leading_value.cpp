#include <functional>

int difference_from(int base, int value) {
    return value - base;
}

std::function<int(int)> solve(int base) {
    // Finish: return a unary callable that subtracts base from its argument
}
