#include <functional>
#include <vector>

std::vector<std::reference_wrapper<int>> solve(std::vector<int>& values) {
    // Finish: return copyable aliases to every element without copying the integers
}
