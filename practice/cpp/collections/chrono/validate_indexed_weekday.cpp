#include <chrono>

bool solve(
    std::chrono::year_month value,
    std::chrono::weekday weekday,
    unsigned index) {
    // Finish: return whether this occurrence of the weekday exists in the month
}
