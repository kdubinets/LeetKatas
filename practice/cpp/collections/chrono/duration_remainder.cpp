#include <chrono>

std::chrono::milliseconds solve(
    std::chrono::milliseconds elapsed,
    std::chrono::seconds interval) {
    // Finish: return the time left after removing all complete intervals
}
