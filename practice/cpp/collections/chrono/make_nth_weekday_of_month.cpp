#include <chrono>

std::chrono::year_month_weekday solve(
    std::chrono::year_month value,
    std::chrono::weekday weekday,
    unsigned index) {
    // Finish: represent the indexed weekday within this year and month
}
