#include <chrono>

std::chrono::year_month_weekday_last solve(
    std::chrono::year_month value,
    std::chrono::weekday weekday) {
    // Finish: represent the final occurrence of weekday within this year and month
}
