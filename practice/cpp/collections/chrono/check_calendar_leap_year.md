# Name

Check a Calendar Leap Year

# Description

Return whether a valid `std::chrono::year` is a leap year according to the civil calendar represented by the chrono calendar types.

# Solution

```cpp
return value.is_leap();
```
