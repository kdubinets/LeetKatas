#include <chrono>

std::chrono::year_month_day solve(
    std::chrono::year_month_day date,
    std::chrono::days offset) {
    // Finish: return the valid civil date offset by the exact number of days
}
