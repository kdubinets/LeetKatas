#include <chrono>

std::chrono::year_month_day solve(
    std::chrono::year_month_day date,
    std::chrono::weekday requested) {
    // Finish: return the first requested weekday that is not before date
}
