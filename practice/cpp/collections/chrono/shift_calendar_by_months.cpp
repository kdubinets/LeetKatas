#include <chrono>

std::chrono::year_month_day solve(
    std::chrono::year_month_day date,
    std::chrono::months offset) {
    // Finish: shift the year and month fields by offset while retaining the day field
}
