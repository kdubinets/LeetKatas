#include <chrono>

std::chrono::steady_clock::duration solve(
    std::chrono::steady_clock::time_point start,
    std::chrono::steady_clock::time_point finish) {
    // Finish: return the signed elapsed duration from start to finish
}
