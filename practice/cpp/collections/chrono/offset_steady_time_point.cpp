#include <chrono>

std::chrono::steady_clock::time_point solve(
    std::chrono::steady_clock::time_point start,
    std::chrono::milliseconds delay) {
    // Finish: return the instant delay milliseconds after start
}
