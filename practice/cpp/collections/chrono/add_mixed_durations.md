# Name

Add Mixed Durations

# Description

Add an integral seconds duration and an integral milliseconds duration, returning the exact total as milliseconds. This trains mixed-period duration arithmetic through the automatically selected common representation.

# Solution

```cpp
return whole + extra;
```
