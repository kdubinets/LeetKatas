#include <charconv>
#include <optional>
#include <string_view>

std::optional<int> solve(std::string_view& remaining) {
    // Finish: return the leading decimal integer and advance the input past the consumed characters
}
