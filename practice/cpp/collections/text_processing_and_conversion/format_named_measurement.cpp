#include <format>
#include <string>
#include <string_view>

std::string solve(std::string_view name, double value) {
    // Finish: return the name and value separated by a colon, with the value shown to two decimal places
}
