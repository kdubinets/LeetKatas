#include <array>
#include <charconv>
#include <string>

std::string solve(double value, int precision) {
    // Finish: return the value in fixed notation with the requested digits after the decimal point
}
