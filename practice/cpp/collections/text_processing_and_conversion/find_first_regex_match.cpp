#include <cstddef>
#include <optional>
#include <regex>
#include <string>
#include <utility>

std::optional<std::pair<std::size_t, std::size_t>> solve(
    const std::string& text, const std::regex& pattern) {
    // Finish: return the starting index and length of the first match when one exists
}
