#include <regex>
#include <string>
#include <vector>

std::vector<std::string> solve(
    const std::string& text, const std::regex& pattern) {
    // Finish: return every non-overlapping match in encounter order
}
