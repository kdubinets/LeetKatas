#include <optional>
#include <regex>
#include <string_view>

std::optional<std::regex> solve(std::string_view expression) {
    // Finish: return a reusable case-insensitive ECMAScript pattern or an empty result when its syntax is invalid
}
