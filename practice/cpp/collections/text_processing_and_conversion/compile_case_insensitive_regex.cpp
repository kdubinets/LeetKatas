#include <optional>
#include <regex>
#include <string_view>

std::optional<std::regex> solve(std::string_view expression) {
    // Finish: return a reusable case-insensitive pattern or an empty result when the expression is invalid
}
