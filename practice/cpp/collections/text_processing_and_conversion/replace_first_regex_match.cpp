#include <regex>
#include <string>

std::string solve(
    const std::string& text,
    const std::regex& pattern,
    const std::string& replacement) {
    // Finish: return a copy with only the first match replaced by the replacement text
}
