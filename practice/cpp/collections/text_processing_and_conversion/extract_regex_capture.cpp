#include <cstddef>
#include <optional>
#include <regex>
#include <string>

std::optional<std::string> solve(
    const std::string& text,
    const std::regex& pattern,
    std::size_t group) {
    // Finish: return the requested capture from the first match when that capture participated
}
