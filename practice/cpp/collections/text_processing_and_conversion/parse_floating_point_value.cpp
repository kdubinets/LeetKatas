#include <charconv>
#include <optional>
#include <string_view>

std::optional<double> solve(std::string_view text) {
    // Finish: return the floating-point value only when the complete input is valid
}
