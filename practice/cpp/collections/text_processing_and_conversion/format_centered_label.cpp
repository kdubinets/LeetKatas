#include <format>
#include <string>
#include <string_view>

std::string solve(std::string_view label, int width) {
    // Finish: return the label centered in a field of asterisks with the requested minimum width
}
