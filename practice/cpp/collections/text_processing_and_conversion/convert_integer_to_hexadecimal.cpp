#include <array>
#include <charconv>
#include <limits>
#include <string>

std::string solve(unsigned int value) {
    // Finish: return the lowercase hexadecimal digits for the value without a prefix
}
