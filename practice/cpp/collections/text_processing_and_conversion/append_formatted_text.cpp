#include <format>
#include <iterator>
#include <string>
#include <string_view>

void solve(std::string& output, std::string_view label, int value) {
    // Finish: append the label and integer separated by an equals sign
}
