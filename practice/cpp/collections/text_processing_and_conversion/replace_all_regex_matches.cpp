#include <regex>
#include <string>

std::string solve(
    const std::string& text,
    const std::regex& pattern,
    const std::string& replacement) {
    // Finish: return a copy with every non-overlapping match replaced by the replacement text
}
