#include <utility>
#include <vector>

struct Batch {
    std::vector<int> values;
};

void solve(Batch& destination, std::vector<int>& source) {
    // Finish: replace the stored values with source without copying its elements
}
