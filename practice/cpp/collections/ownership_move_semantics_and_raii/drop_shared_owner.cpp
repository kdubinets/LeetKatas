#include <memory>

struct Resource {
    int id;
};

void solve(std::shared_ptr<Resource>& owner) {
    // Finish: relinquish this ownership stake while leaving any other owners and the resource they share unaffected
}
