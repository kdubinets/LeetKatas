#include <optional>
#include <string>
#include <utility>

void solve(std::optional<std::string>& destination, std::string& source) {
    // Finish: replace destination with the source string without copying it
}
