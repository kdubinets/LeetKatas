#include <string>
#include <string_view>

struct Name {
    std::string value;
};

struct NameOrder {
    // Finish: order every supported pair of stored names and non-owning lookup keys consistently
};
