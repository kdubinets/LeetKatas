#include <bitset>
#include <cstddef>

std::bitset<32> solve(std::bitset<32> bits, std::size_t count) {
    // Finish: shift the bits toward higher positions and fill the vacated positions with zeroes
}
