#include <bitset>
#include <cstddef>

std::bitset<32> solve(std::bitset<32> bits, std::size_t count) {
    // Finish: shift the bits toward higher positions, fill the vacated positions with zeroes, and produce all zeroes when the count reaches the width
}
