#include <algorithm>
#include <cstddef>
#include <cstdint>
#include <random>
#include <vector>

std::vector<int> solve(std::size_t count, int low, int high, std::uint32_t seed) {
    // Finish: return the requested number of generated integers within the inclusive bounds
}
