#include <algorithm>
#include <cstddef>
#include <cstdint>
#include <random>
#include <vector>

std::vector<int> solve(std::size_t count, int low, int high, std::uint32_t seed) {
    // Finish: use the supplied seed to return the requested number of uniformly generated integers within the inclusive bounds
}
