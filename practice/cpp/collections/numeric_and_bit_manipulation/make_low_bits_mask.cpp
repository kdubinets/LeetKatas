#include <cstdint>
#include <limits>

std::uint32_t solve(unsigned int count) {
    // Finish: return a value whose requested number of least-significant bits are set, producing zero for none and all ones for the full width
}
