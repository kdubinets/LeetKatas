#include <cmath>

double solve(double start, double end, double amount) {
    // Finish: return the value at the requested interpolation amount between the endpoints, extrapolating when the amount is outside zero through one
}
