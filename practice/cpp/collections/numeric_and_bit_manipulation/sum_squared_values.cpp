#include <functional>
#include <numeric>
#include <vector>

long long solve(const std::vector<int>& values) {
    // Finish: return the widened sum of the square of every value, using zero for an empty input
}
