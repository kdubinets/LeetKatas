#include <cstdint>
#include <limits>

std::uint32_t solve(std::uint32_t value, unsigned int offset, unsigned int width) {
    // Finish: return the requested bit field shifted down to the least-significant position, including an empty field or the complete value when requested
}
