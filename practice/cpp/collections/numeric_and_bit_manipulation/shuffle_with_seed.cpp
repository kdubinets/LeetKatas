#include <algorithm>
#include <cstdint>
#include <random>
#include <vector>

std::vector<int> solve(std::vector<int> values, std::uint32_t seed) {
    // Finish: reorder the copied values pseudorandomly using the supplied seed and return them
}
