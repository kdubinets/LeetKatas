#include <cstdint>
#include <limits>

std::uint32_t solve(
    std::uint32_t value,
    std::uint32_t replacement,
    unsigned int offset,
    unsigned int width) {
    // Finish: replace the requested field with the low bits of the replacement and preserve every other bit
}
