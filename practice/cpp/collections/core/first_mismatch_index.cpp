#include <algorithm>
#include <cstddef>
#include <optional>
#include <vector>

std::optional<std::size_t> solve(const std::vector<int>& left,
                                 const std::vector<int>& right) {
    // Finish: return the first index where the inputs differ, using the shorter length for an exact prefix, or no index when equal
}
