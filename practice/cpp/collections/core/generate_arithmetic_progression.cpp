#include <algorithm>
#include <cstddef>
#include <vector>

std::vector<int> solve(std::size_t count, int first, int step) {
    std::vector<int> result(count);
    // Finish: fill result with count representable values beginning at first and increasing by step
    return result;
}
