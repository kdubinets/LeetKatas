# Name

Fill Fixed Array

# Description

Mutate a fixed `std::array<int, 8>` so every element equals the supplied integer. This exercise covers assigning one value across an entire fixed-size range.

# Solution

```cpp
std::ranges::fill(values, value);
```
