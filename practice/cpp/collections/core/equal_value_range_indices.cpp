#include <algorithm>
#include <cstddef>
#include <utility>
#include <vector>

std::pair<std::size_t, std::size_t> solve(
    const std::vector<int>& sorted_values,
    int target) {
    // Finish: in this ascending vector, return the half-open index range of target occurrences, or its empty insertion range when absent
}
