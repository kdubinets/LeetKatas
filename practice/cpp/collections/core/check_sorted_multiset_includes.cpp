#include <algorithm>
#include <vector>

bool solve(const std::vector<int>& values,
           const std::vector<int>& required) {
    // Finish: for these ascending inputs, return whether values contains every required occurrence, including duplicate counts
}
