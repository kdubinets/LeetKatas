#include <cstddef>
#include <string_view>

std::string_view solve(std::string_view text, std::size_t count) {
    // Finish: for valid count no greater than the text size, return a view of the same characters after removing the first count
}
