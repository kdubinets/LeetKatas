#include <algorithm>
#include <iterator>
#include <ranges>
#include <vector>

std::vector<int> solve(const std::vector<int>& values) {
    std::vector<int> result;
    result.reserve(values.size());
    // Finish: copy the input into result in reverse order without modifying it
    return result;
}
