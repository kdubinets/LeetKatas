#include <map>
#include <optional>
#include <utility>

std::optional<std::pair<int, int>> solve(const std::map<int, int>& values,
                                         int key) {
    // Finish: return the first entry whose key is at least key, or no entry when absent
}
