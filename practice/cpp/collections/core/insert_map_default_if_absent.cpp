#include <map>
#include <string>

bool solve(std::map<std::string, int>& values,
           const std::string& key,
           int initial_value) {
    // Finish: add the key and value only when the key is absent, and report whether insertion occurred
}
