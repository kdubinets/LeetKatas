#include <string>

bool solve(std::string& text,
           const std::string& target,
           const std::string& replacement) {
    // Finish: replace the first occurrence of the nonempty target and report whether it was found, leaving text unchanged when absent
}
