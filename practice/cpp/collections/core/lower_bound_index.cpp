#include <algorithm>
#include <cstddef>
#include <vector>

std::size_t solve(const std::vector<int>& sorted_values, int target) {
    // Finish: in this ascending vector, return the index of the first value not less than target, or the input size when none exists
}
