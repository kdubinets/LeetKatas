# Name

Clamp to Inclusive Range

# Description

Given an integer value and valid bounds satisfying `low <= high`, return the value limited to the inclusive interval `[low, high]`. This exercise covers concise bounded-value selection.

# Solution

```cpp
return std::clamp(value, low, high);
```
