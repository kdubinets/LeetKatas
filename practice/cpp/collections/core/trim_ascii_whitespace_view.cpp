#include <string_view>

std::string_view solve(std::string_view text) {
    // Finish: return the portion excluding leading and trailing ASCII space, tab, newline, carriage return, form-feed, and vertical-tab characters without copying
}
