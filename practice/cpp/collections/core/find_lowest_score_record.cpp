#include <algorithm>
#include <vector>

struct Record {
    int id;
    int score;
};

int solve(const std::vector<Record>& records) {
    // Finish: return the id of the first record having the lowest score in the nonempty input
}
