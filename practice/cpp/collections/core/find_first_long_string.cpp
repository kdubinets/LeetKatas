#include <algorithm>
#include <cstddef>
#include <optional>
#include <string>
#include <vector>

std::optional<std::string> solve(const std::vector<std::string>& values,
                                 std::size_t minimum_length) {
    // Finish: return the first string whose length is at least minimum_length, or no value when none does
}
