#include <functional>
#include <queue>
#include <vector>

int solve(const std::vector<int>& values) {
    // Finish: build a priority-based ordering structure from the nonempty input and return its smallest value
}
