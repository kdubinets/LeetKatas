# Name

Check Unordered Map Membership

# Description

Return whether a `std::string` key is present in a const `std::unordered_map<std::string, int>`. The map must not be modified. This exercise covers direct membership testing in an associative container.

# Solution

```cpp
return values.contains(key);
```
