#include <cstddef>
#include <vector>

std::vector<std::size_t> solve(const std::vector<int>& values, int target) {
    std::vector<std::size_t> indices;
    // Finish: collect every index whose value equals target in ascending index order
    return indices;
}
