#include <algorithm>
#include <cstddef>
#include <vector>

int solve(std::vector<int>& values, std::size_t k) {
    // Finish: for valid index k in this nonempty input, rearrange as needed and return the value that would appear at k in ascending order
}
