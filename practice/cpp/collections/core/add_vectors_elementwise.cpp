#include <algorithm>
#include <vector>

std::vector<long long> solve(const std::vector<int>& left,
                             const std::vector<int>& right) {
    std::vector<long long> result(left.size());
    // Finish: given equal-length inputs, fill result with long long sums of corresponding values
    return result;
}
