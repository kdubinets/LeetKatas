# Name

Replace Vector Value

# Description

Mutate an integer vector by replacing every element equal to a supplied old value with a supplied new value, leaving all other elements unchanged. This exercise covers whole-range value replacement.

# Solution

```cpp
std::ranges::replace(values, old_value, new_value);
```
