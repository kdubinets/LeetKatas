#include <algorithm>
#include <iterator>
#include <vector>

std::vector<int> solve(const std::vector<int>& left,
                       const std::vector<int>& right) {
    std::vector<int> result;
    // Finish: for these ascending inputs, return left values remaining after each right occurrence removes at most one equal value
    return result;
}
