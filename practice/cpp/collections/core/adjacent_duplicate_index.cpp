#include <algorithm>
#include <cstddef>
#include <optional>
#include <vector>

std::optional<std::size_t> solve(const std::vector<int>& values) {
    // Finish: return the index of the first value equal to the value after it, or no index when absent
}
