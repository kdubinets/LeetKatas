#include <queue>
#include <vector>

std::vector<int> solve(std::priority_queue<int> values) {
    std::vector<int> result;
    result.reserve(values.size());
    // Finish: remove every copied heap value into result from greatest to smallest
    return result;
}
