#include <cstddef>
#include <numeric>
#include <vector>

std::vector<std::size_t> solve(std::size_t count) {
    std::vector<std::size_t> indices(count);
    // Finish: fill the output with consecutive indices starting at zero
    return indices;
}
