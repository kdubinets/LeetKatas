#include <numeric>
#include <vector>

std::vector<long long> solve(const std::vector<int>& values) {
    std::vector<long long> result(values.size());
    // Finish: fill each position with the long long sum of all input values before it, making the first output zero
    return result;
}
