#include <string>
#include <unordered_set>

bool solve(std::unordered_set<std::string>& values,
           const std::string& value) {
    // Finish: add the value and report whether it was newly added
}
