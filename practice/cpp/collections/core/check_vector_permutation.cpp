#include <algorithm>
#include <vector>

bool solve(const std::vector<int>& left, const std::vector<int>& right) {
    // Finish: return whether both inputs contain the same values with the same multiplicities in any order
}
