#include <charconv>
#include <optional>
#include <string_view>
#include <system_error>

std::optional<int> solve(std::string_view text) {
    // Finish: return a decimal integer only for nonempty text that is fully valid and in range; reject whitespace, a leading plus, and trailing characters
}
