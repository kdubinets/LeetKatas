#include <map>
#include <optional>
#include <string>

std::optional<int> solve(const std::map<std::string, int>& values,
                         const std::string& key) {
    // Finish: return the mapped value when the key exists, or no value otherwise
}
