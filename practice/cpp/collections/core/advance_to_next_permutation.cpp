#include <algorithm>
#include <vector>

bool solve(std::vector<int>& values) {
    // Finish: advance to the next lexicographical arrangement, or rearrange to the smallest and return false when none exists
}
