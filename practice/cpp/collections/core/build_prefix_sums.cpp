#include <functional>
#include <numeric>
#include <vector>

std::vector<long long> solve(const std::vector<int>& values) {
    std::vector<long long> result(values.size());
    // Finish: fill result with long long sums through each corresponding input position
    return result;
}
