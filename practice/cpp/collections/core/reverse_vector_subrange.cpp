#include <algorithm>
#include <cstddef>
#include <vector>

void solve(std::vector<int>& values,
           std::size_t first,
           std::size_t last) {
    // Finish: reverse the valid half-open index interval [first, last), leaving values outside it unchanged
}
