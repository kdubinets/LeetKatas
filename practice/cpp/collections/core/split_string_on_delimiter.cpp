#include <string>
#include <vector>

std::vector<std::string> solve(const std::string& text, char delimiter) {
    std::vector<std::string> parts;
    // Finish: split text at every delimiter while preserving empty parts, including one empty part for empty text
    return parts;
}
