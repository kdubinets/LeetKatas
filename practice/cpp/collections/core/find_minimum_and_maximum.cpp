#include <algorithm>
#include <utility>
#include <vector>

std::pair<int, int> solve(const std::vector<int>& values) {
    // Finish: return the smallest value followed by the largest value from the nonempty input
}
