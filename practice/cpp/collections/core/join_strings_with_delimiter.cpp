#include <cstddef>
#include <string>
#include <vector>

std::string solve(const std::vector<std::string>& values,
                  const std::string& delimiter) {
    std::string result;
    // Finish: join the strings in order with one delimiter between adjacent values
    return result;
}
