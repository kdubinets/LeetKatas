#include <algorithm>
#include <vector>

struct Record {
    int id;
    int score;
};

void solve(std::vector<Record>& records) {
    // Finish: order the records by ascending id, without using score to affect their ordering
}
