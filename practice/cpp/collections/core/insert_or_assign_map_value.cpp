#include <map>
#include <string>

bool solve(std::map<std::string, int>& values,
           const std::string& key,
           int value) {
    // Finish: store value for key, replacing an existing value, and report whether the key was new
}
