#include <algorithm>
#include <cstddef>
#include <iterator>
#include <string>
#include <vector>

std::vector<std::size_t> solve(const std::vector<std::string>& values) {
    std::vector<std::size_t> lengths;
    lengths.reserve(values.size());
    // Finish: fill lengths with each input string's character count in the same order
    return lengths;
}
