#include <cstddef>
#include <vector>

void solve(std::vector<int>& values, std::size_t index, int value) {
    // Finish: insert value before the valid position index, which may equal the current size
}
