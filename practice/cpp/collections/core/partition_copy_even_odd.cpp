#include <algorithm>
#include <iterator>
#include <utility>
#include <vector>

std::pair<std::vector<int>, std::vector<int>> solve(
    const std::vector<int>& values) {
    std::pair<std::vector<int>, std::vector<int>> result;
    // Finish: copy even values to the first output and odd values to the second, preserving order
    return result;
}
