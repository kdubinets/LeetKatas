#include <string>
#include <unordered_map>

void solve(std::unordered_map<std::string, int>& frequencies,
           const std::unordered_map<std::string, int>& additional) {
    // Finish: add every additional count into the corresponding frequency, creating it when absent
}
