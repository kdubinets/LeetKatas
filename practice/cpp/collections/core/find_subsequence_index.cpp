#include <algorithm>
#include <cstddef>
#include <optional>
#include <vector>

std::optional<std::size_t> solve(const std::vector<int>& values,
                                 const std::vector<int>& pattern) {
    // Finish: return where the first contiguous pattern occurs, using index zero for an empty pattern or no index when absent
}
