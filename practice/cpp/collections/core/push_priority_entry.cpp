#include <queue>
#include <utility>
#include <vector>

void solve(std::priority_queue<std::pair<int, int>>& tasks,
           int priority,
           int id) {
    // Finish: add an entry containing the priority followed by the id
}
