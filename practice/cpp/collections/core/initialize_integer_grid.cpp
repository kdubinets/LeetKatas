#include <cstddef>
#include <vector>

std::vector<std::vector<int>> solve(std::size_t rows,
                                    std::size_t columns,
                                    int initial_value) {
    // Finish: return a rows-by-columns grid with every cell set to initial_value
}
