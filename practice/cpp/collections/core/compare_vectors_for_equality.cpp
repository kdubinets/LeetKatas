#include <algorithm>
#include <vector>

bool solve(const std::vector<int>& left, const std::vector<int>& right) {
    // Finish: return whether both inputs contain the same values in the same order
}
