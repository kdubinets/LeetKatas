#include <string>
#include <unordered_map>

void solve(std::unordered_map<std::string, int>& frequencies,
           const std::string& word) {
    // Finish: increase the word's count by one, starting it at one when absent
}
