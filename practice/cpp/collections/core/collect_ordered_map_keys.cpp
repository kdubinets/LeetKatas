#include <map>
#include <vector>

std::vector<int> solve(const std::map<int, int>& values) {
    std::vector<int> keys;
    keys.reserve(values.size());
    // Finish: collect every key in ascending key order
    return keys;
}
