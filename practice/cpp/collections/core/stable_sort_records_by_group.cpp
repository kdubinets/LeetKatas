#include <algorithm>
#include <vector>

struct Record {
    int group;
    int id;
};

void solve(std::vector<Record>& records) {
    // Finish: order records by ascending group while retaining original order among equal groups
}
