#include <algorithm>
#include <iterator>
#include <vector>

std::vector<int> solve(const std::vector<int>& left,
                       const std::vector<int>& right) {
    std::vector<int> result;
    // Finish: for these ascending inputs, return their ascending intersection with each value kept the smaller number of times it occurs
    return result;
}
