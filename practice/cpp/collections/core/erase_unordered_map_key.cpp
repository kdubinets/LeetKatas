#include <string>
#include <unordered_map>

bool solve(std::unordered_map<std::string, int>& values,
           const std::string& key) {
    // Finish: remove key when present and report whether an entry was removed
}
