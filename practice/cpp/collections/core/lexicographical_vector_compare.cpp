#include <algorithm>
#include <vector>

bool solve(const std::vector<int>& left, const std::vector<int>& right) {
    // Finish: return whether left comes before right by comparing corresponding values, with a matching shorter prefix first
}
