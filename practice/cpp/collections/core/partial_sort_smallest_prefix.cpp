#include <algorithm>
#include <cstddef>
#include <vector>

void solve(std::vector<int>& values, std::size_t count) {
    // Finish: for valid count no greater than the size, place the smallest count values in ascending order at the front; later values need not be ordered
}
