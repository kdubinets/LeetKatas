#include <cstddef>
#include <vector>

void solve(std::vector<int>& values,
           std::size_t new_size,
           int added_value) {
    // Finish: resize to new_size, discarding trailing values when shrinking and using added_value for new elements
}
