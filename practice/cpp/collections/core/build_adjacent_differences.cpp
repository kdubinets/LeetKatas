#include <numeric>
#include <vector>

std::vector<long long> solve(const std::vector<int>& values) {
    std::vector<long long> result(values.size());
    // Finish: return an empty result for empty input; otherwise keep the first value, then store long long differences from the preceding value
    return result;
}
