#include <stack>
#include <vector>

std::vector<int> solve(std::stack<int>& values) {
    std::vector<int> result;
    // Finish: remove every stack value into result from top to bottom
    return result;
}
