#include <algorithm>
#include <iterator>
#include <vector>

std::vector<int> solve(const std::vector<int>& left,
                       const std::vector<int>& right) {
    std::vector<int> result;
    // Finish: for these ascending inputs, return their ascending union with each value kept the larger number of times it occurs
    return result;
}
