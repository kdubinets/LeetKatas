# Name

Count Set Bits

# Description

Return the number of one bits in the binary representation of an `unsigned int`. This exercise covers the C++20 bit-counting utility for unsigned integer types.

# Solution

```cpp
return std::popcount(value);
```
