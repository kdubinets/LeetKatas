#include <string>
#include <type_traits>
#include <variant>

std::string solve(const std::variant<int, std::string>& value) {
    // Finish: return text unchanged or convert the integer to decimal text
}
