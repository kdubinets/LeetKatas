#include <cstddef>
#include <string>
#include <variant>

std::string& solve(std::variant<int, std::string>& value, std::size_t count, char ch) {
    // Finish: replace the active value with text containing count copies of ch and return that text
}
