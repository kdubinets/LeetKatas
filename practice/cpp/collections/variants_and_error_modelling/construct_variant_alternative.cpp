#include <string>
#include <utility>
#include <variant>

using Value = std::variant<int, std::string>;

Value solve(std::string text) {
    // Finish: return a value whose text alternative contains the supplied text
}
