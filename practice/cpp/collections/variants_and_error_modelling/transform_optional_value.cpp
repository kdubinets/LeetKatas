#include <cstddef>
#include <optional>
#include <string>

std::optional<std::size_t> solve(const std::optional<std::string>& text) {
    // Finish: return the text length when text is present and otherwise no value
}
