#include <optional>
#include <string>

std::optional<int> parse_port(const std::string&);

std::optional<int> solve(const std::optional<std::string>& text) {
    // Finish: parse the text only when it is present and preserve either kind of absence
}
