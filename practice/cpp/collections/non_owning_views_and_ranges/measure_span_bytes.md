# Name

Measure Span Bytes

# Description

Return the total byte size of all `double` elements viewed by a read-only dynamic span. This exercise covers obtaining a span's storage size rather than its element count.

# Solution

```cpp
return values.size_bytes();
```
