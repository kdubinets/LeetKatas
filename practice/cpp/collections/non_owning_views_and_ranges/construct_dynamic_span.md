# Name

Construct a Dynamic Span

# Description

Return a mutable dynamic-extent `std::span<int>` over every element of a mutable `std::vector<int>`. The vector retains ownership and no elements may be copied. This exercise covers constructing a non-owning contiguous view from an owning container.

# Solution

```cpp
return values;
```
