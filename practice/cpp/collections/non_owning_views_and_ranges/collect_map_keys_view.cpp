#include <algorithm>
#include <iterator>
#include <map>
#include <ranges>
#include <string>
#include <vector>

std::vector<std::string> solve(const std::map<std::string, int>& values) {
    std::vector<std::string> result;
    result.reserve(values.size());
    // Finish: copy only the keys in their iteration order
    return result;
}
