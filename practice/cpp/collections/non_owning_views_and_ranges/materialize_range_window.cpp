#include <algorithm>
#include <cstddef>
#include <iterator>
#include <ranges>
#include <vector>

std::vector<int> solve(const std::vector<int>& values,
                       std::size_t offset,
                       std::size_t count) {
    std::vector<int> result;
    // Finish: copy at most count values beginning at offset, naturally returning fewer when the requested window extends past the input
    return result;
}
