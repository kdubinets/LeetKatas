#include <algorithm>
#include <cstddef>
#include <span>

std::span<const int> solve(std::span<const int> values, int target) {
    // Finish: return the view from the first matching value through the end, or an empty view positioned at the end when absent
}
