#include <algorithm>
#include <iterator>
#include <ranges>
#include <vector>

std::vector<int> solve(const std::vector<std::vector<int>>& rows) {
    std::vector<int> result;
    // Finish: copy every row's values into one result in row order
    return result;
}
