#include <cstddef>
#include <string_view>

void solve(std::string_view& text, std::size_t count) {
    // Finish: for a valid count, shorten the view by discarding that many characters from the end
}
