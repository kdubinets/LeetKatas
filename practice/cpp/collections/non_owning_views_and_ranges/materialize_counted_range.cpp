#include <algorithm>
#include <cstddef>
#include <iterator>
#include <ranges>
#include <vector>

std::vector<int> solve(const std::vector<int>& values,
                       std::size_t start,
                       std::size_t count) {
    std::vector<int> result;
    result.reserve(count);
    // Finish: for a valid indexed segment, copy exactly count values beginning at start
    return result;
}
