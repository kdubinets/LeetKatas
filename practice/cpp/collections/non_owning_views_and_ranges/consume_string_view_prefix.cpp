#include <cstddef>
#include <string_view>

std::string_view solve(std::string_view& remaining, std::size_t count) {
    // Finish: for a valid count, return the first count characters and advance remaining beyond them
}
