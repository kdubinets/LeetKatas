#include <algorithm>
#include <cstddef>
#include <iterator>
#include <ranges>
#include <vector>

std::vector<int> solve(int first, std::size_t count) {
    std::vector<int> result;
    result.reserve(count);
    // Finish: return exactly count consecutive representable integers beginning at first
    return result;
}
