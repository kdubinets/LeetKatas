#include <cstddef>
#include <span>
#include <string>
#include <vector>

std::span<const std::string> solve(const std::vector<std::string>& values,
                                   std::size_t start) {
    // Finish: for a valid start position, return a read-only view from that element through the end
}
