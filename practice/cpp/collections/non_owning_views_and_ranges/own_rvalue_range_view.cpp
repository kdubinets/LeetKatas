#include <ranges>
#include <string>
#include <utility>
#include <vector>

auto solve(std::vector<std::string> values) {
    // Finish: return an iterable object that owns these strings and keeps their storage alive
}
