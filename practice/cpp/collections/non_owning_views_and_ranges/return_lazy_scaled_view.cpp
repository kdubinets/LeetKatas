#include <ranges>
#include <vector>

auto solve(const std::vector<int>& values, int factor) {
    // Finish: return an iterable result that produces each value multiplied by factor using long long arithmetic without copying the input
}
